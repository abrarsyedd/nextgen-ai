{
  "name": "nextgen-ai-backend",
  "version": "1.0.0",
  "main": "app.js",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.0"
  },
  "scripts": {
    "start": "echo 'Waiting for MySQL...' && while ! nc -z db 3306; do sleep 1; done && echo 'MySQL is up! Starting Server...' && node app.js"
  }
}